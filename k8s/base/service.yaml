apiVersion: v1
kind: Service
metadata:
  name: whatsapp-api
  namespace: whatsapp
  labels:
    app.kubernetes.io/name: whatsapp-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: whatsapp-api
