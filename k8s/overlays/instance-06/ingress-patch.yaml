apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whatsapp-api
spec:
  rules:
    - host: whatsapp-06.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: instance-06-whatsapp-api
                port:
                  name: http
