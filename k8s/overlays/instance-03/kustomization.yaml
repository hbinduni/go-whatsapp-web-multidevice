apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: whatsapp-03

namePrefix: instance-03-

resources:
  - ../../base
  - postgres.yaml

patches:
  - path: namespace-patch.yaml
  - path: secret-patch.yaml
  - path: ingress-patch.yaml

commonLabels:
  app.kubernetes.io/instance: instance-03

commonAnnotations:
  whatsapp.io/instance-id: "03"
